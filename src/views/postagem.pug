if resultado

    - var opcao = { year: "numeric", month: "long", day: "numeric", weekday: "long" }
    - var data = new Date(resultado.data_post).toLocaleString("pt-BR", opcao)

    div(class="postagem")

        div(class="postagem-nomeusuario") #{ resultado.nomeusuario }

        div(class="postagem-descricao") #{ resultado.descricao }

        img(src=resultado.conteudo, alt="foto do conteudo")

        div(class="postagem-data") #{ data }

else 

    h1 Postagem não encontrada!